pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: pnpm lint-staged

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

pre-push:
  commands:
    test:
      run: pnpm test:run

post-commit:
  commands:
    push:
      run: git push
